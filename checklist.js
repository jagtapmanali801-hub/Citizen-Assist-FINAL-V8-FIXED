function renderChecklist(steps){const c=document.getElementById('checklistContainer');if(!c)return;c.innerHTML='';steps.forEach((step,i)=>{const label=document.createElement('label');label.className='check-item';label.innerHTML=`<input type="checkbox" class="checklist-item" onchange="calculateChecklistProgress()"><span><strong>${typeof t==='function'?t('step_label'):'Step'} ${i+1}:</strong> ${step}</span>`;c.appendChild(label);});calculateChecklistProgress();}
function calculateChecklistProgress(){const items=[...document.querySelectorAll('.checklist-item')];const done=items.filter(x=>x.checked).length;const out=document.getElementById('guideProgressPercent');if(out)out.textContent=(items.length?Math.round(done/items.length*100):0)+'%';}
